Review this pfit session for readiness before JAX translation.

Deterministic preflight report:

$deterministic_report

Runtime session contract (authoritative array indices):

$session_summary

Input YAML:

$input_yaml

Dataset summary:

$dataset_summary

User's stated loss:

$user_loss_contract

Loss expression evidence (literal source expressions, not an equivalence proof):

$loss_evidence

User model:

$user_model

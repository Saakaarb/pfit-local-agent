Validation error:
$validation_error

Session context:
$session_context

Current parsed pfit-new draft, with all non-loss fields frozen:
$current_spec

Previous loss_body:
$loss_body

Return only corrected JSON:
{"loss_body": "..."}

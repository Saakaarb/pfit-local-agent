[pytest]
addopts = -m "not slow and not local_llm"
markers =
    unit: fast unit tests for deterministic code
    contract: generated-code and prompt contract tests
    workflow: orchestrator and end-to-end workflow tests
    cli: command-line interface tests
    slow: slow numerical fitting tests
    local_llm: tests that require a local LLM server
